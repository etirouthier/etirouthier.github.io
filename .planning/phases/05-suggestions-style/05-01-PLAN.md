---
phase: 05-suggestions-style
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app.py
  - .streamlit/config.toml
autonomous: false
requirements:
  - ACCU-02
  - BRAND-02

must_haves:
  truths:
    - "4 boutons de suggestion affichent des questions recruteur spécifiques au premier chargement"
    - "Cliquer sur un chip déclenche le pipeline RAG avec la question affichée (label = args)"
    - "Les boutons s'affichent avec un style pill (bords pleinement arrondis)"
    - "L'app se charge sans StreamlitAPIException (st.set_page_config en première position)"
    - "L'onglet navigateur affiche 'Etienne Routhier — Dossier de Compétences'"
  artifacts:
    - path: "app.py"
      provides: "st.set_page_config en première position, SUGGESTIONS constant, layout 4 colonnes"
      contains: "st.set_page_config"
    - path: ".streamlit/config.toml"
      provides: "Theming pill buttons via buttonRadius = full"
      contains: "buttonRadius"
  key_links:
    - from: "app.py SUGGESTIONS list"
      to: "st.button args=(question,)"
      via: "variable reference — no string duplication"
      pattern: "args=\\(question,"
    - from: ".streamlit/config.toml"
      to: "Streamlit button rendering"
      via: "buttonRadius = \"full\" in [theme] section"
      pattern: "buttonRadius"
    - from: "st.set_page_config"
      to: "@st.cache_resource decorator"
      via: "placement before decorator — ordering constraint"
      pattern: "set_page_config"
---

<objective>
Mettre à jour app.py et créer .streamlit/config.toml pour livrer les 4 chips recruteur ciblées et le style pill sur une app en production.

Purpose: Un recruteur voit dès le premier chargement 4 suggestions couvrant ses questions clés (stack technique, types de missions, fit mission, disponibilité/TJM), avec des boutons arrondis signalant un design professionnel.

Output: app.py mis à jour (set_page_config + 4 chips), .streamlit/config.toml créé (buttonRadius = "full"), app déployée et validée visuellement sur https://etirouthierappio.streamlit.app/
</objective>

<execution_context>
@/home/codespace/.claude/get-shit-done/workflows/execute-plan.md
@/home/codespace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@app.py

<interfaces>
<!-- État actuel de app.py — contexte complet pour l'exécuteur -->
<!-- app.py fait 103 lignes. Structure exacte des sections à modifier : -->

Ligne 1-9 — Import block (ne pas modifier) :
```python
import streamlit as st
from dotenv import load_dotenv
from langchain_community.vectorstores import FAISS
from langchain_core.messages import SystemMessage, HumanMessage, AIMessage
from langchain_mistralai import ChatMistralAI, MistralAIEmbeddings
from config import EMBEDDING_MODEL, LLM_MODEL, K_RETRIEVED, FAISS_INDEX_PATH

load_dotenv()
```

Ligne 11-17 — SYSTEM_PROMPT (ne pas modifier)

Ligne 20 — @st.cache_resource (CONTRAINTE : st.set_page_config doit etre AVANT cette ligne) :
```python
@st.cache_resource(show_spinner="Chargement de la base de connaissances...")
def load_vectorstore():
    ...
```

Lignes 49-62 — Section chips a remplacer (section complete) :
```python
if len(st.session_state.messages) == 0:
    col1, col2 = st.columns(2)
    with col1:
        st.button(
            "Quelles sont vos principales competences ?",
            on_click=inject_question,
            args=("Quelles sont vos principales competences ?",),
        )
    with col2:
        st.button(
            "En quoi pouvez-vous m'aider sur mon projet ?",
            on_click=inject_question,
            args=("En quoi pouvez-vous m'aider sur mon projet ?",),
        )
```

Fonction inject_question (lignes 35-36) — ne pas modifier :
```python
def inject_question(q: str):
    st.session_state.pending_question = q
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Modifier app.py — set_page_config et 4 chips recruteur</name>
  <files>app.py</files>
  <action>
Deux modifications dans app.py — meme commit.

**Modification 1 — Inserer st.set_page_config (CONTRAINTE CRITIQUE)**

Inserer le bloc suivant apres `load_dotenv()` (ligne 9) et AVANT le `SYSTEM_PROMPT` et surtout AVANT le `@st.cache_resource` decorator (ligne 20 actuelle). Contrainte hard Streamlit : tout `st.*` call precedant `set_page_config` leve une `StreamlitAPIException` immediate au demarrage.

```python
st.set_page_config(
    page_title="Etienne Routhier — Dossier de Competences",
    page_icon="💼",
    layout="centered",
)
```

Positionner entre `load_dotenv()` et `SYSTEM_PROMPT = (`.

**Modification 2 — Remplacer le bloc 2-chips par le bloc 4-chips**

Ajouter la constante `SUGGESTIONS` juste avant le `if len(st.session_state.messages) == 0:` guard (actuellement ligne 49). Remplacer ensuite l'integralite du bloc `col1, col2 = st.columns(2)...` par la boucle 4-chips.

Constante a ajouter :
```python
SUGGESTIONS = [
    "Quelle est votre stack technique principale ?",
    "Quels types de missions avez-vous realises ?",
    "Etes-vous disponible et quel est votre TJM ?",
    "Seriez-vous un bon fit pour ma mission ?",
]
```

Nouveau bloc chips (remplace l'ancien bloc colonnes) :
```python
if len(st.session_state.messages) == 0:
    cols = st.columns(4)
    for i, (col, question) in enumerate(zip(cols, SUGGESTIONS)):
        with col:
            st.button(
                question,
                key=f"suggestion_{i}",
                on_click=inject_question,
                args=(question,),
            )
```

Points critiques :
- `label` et `args=(question,)` referencent la meme variable `question` — ne jamais dupliquer la string.
- `key=f"suggestion_{i}"` est obligatoire — evite le bug de deduplication de boutons Streamlit sur un meme render pass.
- La constante `SUGGESTIONS` peut etre placee en module-level avant le guard ou juste au-dessus du guard — les deux sont corrects.
- Ne pas modifier inject_question, SYSTEM_PROMPT, ni le reste du pipeline RAG.
  </action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('/workspaces/etienne.routhier/app.py').read()); print('syntax OK')"</automated>
  </verify>
  <done>
- app.py contient `st.set_page_config(` positionne avant `@st.cache_resource`
- app.py contient la constante `SUGGESTIONS` avec 4 questions recruteur
- app.py contient `st.columns(4)` dans le guard `messages == 0`
- Aucune duplication de string entre label et args
- Syntaxe Python valide (verification automatisee passe)
  </done>
</task>

<task type="auto">
  <name>Task 2: Creer .streamlit/config.toml — style pill buttons</name>
  <files>.streamlit/config.toml</files>
  <action>
Creer le fichier `.streamlit/config.toml` avec le bloc `[theme]` suivant :

```toml
[theme]
buttonRadius = "full"
```

Points critiques :
- Le repertoire `.streamlit/` existe deja (contient `secrets.toml`).
- `config.toml` doit etre COMMITE dans le repo — c'est un fichier de configuration non-secret. Verifier que `.gitignore` exclut `secrets.toml` specifiquement, pas l'integralite du dossier `.streamlit/`.
- `buttonRadius = "full"` produit des boutons pill (coins pleinement arrondis). Stable sur Streamlit 1.44–1.55.
- Ne pas ajouter d'autres cles `[theme]` pour l'instant — Phase 6 ajoutera les couleurs si necessaire.
- Ne pas modifier `secrets.toml`.
  </action>
  <verify>
    <automated>python -c "import tomllib; data = tomllib.loads(open('/workspaces/etienne.routhier/.streamlit/config.toml').read()); assert data['theme']['buttonRadius'] == 'full'; print('config.toml OK')"</automated>
  </verify>
  <done>
- Fichier `.streamlit/config.toml` existe avec `[theme]` et `buttonRadius = "full"`
- Validation TOML passe sans erreur
- `.gitignore` n'exclut pas config.toml (verifier : `git check-ignore .streamlit/config.toml` retourne vide)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Validation visuelle sur l'URL live</name>
  <files>https://etirouthierappio.streamlit.app/</files>
  <action>Pousser les commits sur main, attendre le deploiement automatique Streamlit Community Cloud (~60 secondes), puis ouvrir l'URL live pour valider visuellement.</action>
  <what-built>
- app.py : st.set_page_config ajoute (page title "Etienne Routhier — Dossier de Competences"), 4 chips recruteur remplacent les 2 anciens boutons
- .streamlit/config.toml : cree avec buttonRadius = "full"
- Changements commites et pousses sur main — Streamlit Community Cloud deploie automatiquement
  </what-built>
  <how-to-verify>
1. Ouvrir https://etirouthierappio.streamlit.app/ dans un navigateur
2. Verifier l'onglet navigateur : titre doit afficher "Etienne Routhier — Dossier de Competences" (pas "streamlit")
3. Verifier les 4 chips visibles au premier chargement avec les textes exacts :
   - "Quelle est votre stack technique principale ?"
   - "Quels types de missions avez-vous realises ?"
   - "Etes-vous disponible et quel est votre TJM ?"
   - "Seriez-vous un bon fit pour ma mission ?"
4. Verifier le style pill : les boutons ont des coins pleinement arrondis (pas carres)
5. Cliquer sur chaque chip et verifier que la question affichee dans le chat correspond exactement au label du bouton clique
6. Optionnel — tester sur viewport mobile (375px) : verifier que les 4 colonnes restent lisibles (texte non tronque). Si les labels wrappent mal, noter pour ajustement vers 2x st.columns(2).

Si le deploiement n'est pas encore termine, attendre ~60 secondes et recharger.
  </how-to-verify>
  <verify>Checkpoint : validation visuelle approuvee par l'utilisateur</verify>
  <done>Utilisateur a confirme : 4 chips affichent le bon texte, style pill actif, chaque chip declenche le bon RAG, onglet navigateur affiche le bon titre.</done>
  <resume-signal>Taper "approved" si tout est conforme, ou decrire les problemes observes (ex: "chip 3 affiche mauvaise question", "pas de pill style", "overflow mobile")</resume-signal>
</task>

</tasks>

<verification>
Avant de marquer la phase complete, verifier :

1. `python -c "import ast; ast.parse(open('app.py').read()); print('syntax OK')"` — passe sans erreur
2. `python -c "import tomllib; data = tomllib.loads(open('.streamlit/config.toml').read()); assert data['theme']['buttonRadius'] == 'full'"` — passe sans erreur
3. `git check-ignore .streamlit/config.toml` — retourne vide (fichier non ignore par git)
4. `grep -n "set_page_config" app.py` — retourne une ligne AVANT la ligne contenant `@st.cache_resource`
5. `grep -n "SUGGESTIONS" app.py` — retourne la constante avec 4 items
6. Validation visuelle sur live URL (Task 3 checkpoint approuve)
</verification>

<success_criteria>
- L'onglet navigateur affiche "Etienne Routhier — Dossier de Competences" sur la live URL
- 4 chips recruteur visibles au premier chargement avec les 4 textes exacts definis dans SUGGESTIONS
- Cliquer chaque chip declenche le pipeline RAG avec la question correspondante (label = question injectee)
- Boutons s'affichent avec style pill (bords arrondis) — config.toml deploye sur Streamlit Community Cloud
- App se charge sans StreamlitAPIException
</success_criteria>

<output>
After completion, create `.planning/phases/05-suggestions-style/05-01-SUMMARY.md` using the summary template at @/home/codespace/.claude/get-shit-done/templates/summary.md
</output>
