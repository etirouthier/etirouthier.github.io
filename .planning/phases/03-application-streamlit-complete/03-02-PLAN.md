---
phase: 03-application-streamlit-complete
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - app.py
autonomous: false
requirements:
  - RAG-01
  - RAG-02
  - RAG-03
  - RAG-04
  - UI-01
  - UI-02
  - UI-03
  - UI-04
  - UI-05
  - PROMPT-01
  - PROMPT-02
  - PROMPT-03
  - PROMPT-04

must_haves:
  truths:
    - "L'app se charge sans erreur (`streamlit run app.py`) et affiche deux boutons de questions suggérées avant le premier message"
    - "Cliquer sur un bouton ou saisir une question déclenche une réponse en français basée sur le contenu du PDF"
    - "L'historique de la conversation reste visible pendant toute la session et les questions de suivi obtiennent des réponses cohérentes avec le contexte précédent"
    - "En cas de rate limit Mistral (HTTP 429), un message d'erreur en français s'affiche — l'app ne plante pas"
  artifacts:
    - path: "app.py"
      provides: "Application Streamlit complète avec pipeline RAG, session state, boutons suggérés, gestion d'erreur"
      min_lines: 80
      exports: ["load_vectorstore", "SYSTEM_PROMPT"]
  key_links:
    - from: "app.py"
      to: "faiss_index/"
      via: "FAISS.load_local() dans load_vectorstore()"
      pattern: "FAISS\\.load_local"
    - from: "app.py"
      to: "mistral-small-latest"
      via: "ChatMistralAI(model=LLM_MODEL).invoke(messages)"
      pattern: "ChatMistralAI"
    - from: "app.py"
      to: "config.py"
      via: "import EMBEDDING_MODEL, LLM_MODEL, K_RETRIEVED, FAISS_INDEX_PATH"
      pattern: "from config import"

user_setup:
  - service: mistral
    why: "Clé API requise pour MistralAIEmbeddings (chargement index) et ChatMistralAI (génération réponses)"
    env_vars:
      - name: MISTRAL_API_KEY
        source: "console.mistral.ai → API Keys → Create new key"
    local_config:
      - task: "Créer .streamlit/secrets.toml avec MISTRAL_API_KEY = \"sk-...\""
        location: ".streamlit/secrets.toml (déjà dans .gitignore)"
---

<objective>
Implémenter `app.py` — l'application Streamlit complète qui satisfait les 13 requirements de la phase 3. L'app charge l'index FAISS existant, expose une interface de chat, et retourne des réponses en français basées sur le PDF via le pipeline RAG.

Purpose: Livrer la valeur principale du projet — un client potentiel peut interroger le profil professionnel en langage naturel.
Output: `app.py` (~100 lignes), fonctionnel en local avec `streamlit run app.py`
</objective>

<execution_context>
@/home/codespace/.claude/get-shit-done/workflows/execute-plan.md
@/home/codespace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@config.py
@.planning/phases/03-application-streamlit-complete/03-01-SUMMARY.md

<interfaces>
<!-- Constantes depuis config.py — NE PAS dupliquer dans app.py -->
```python
EMBEDDING_MODEL = "mistral-embed"   # Doit être identique à build_index.py
LLM_MODEL = "mistral-small-latest"
K_RETRIEVED = 4
FAISS_INDEX_PATH = "faiss_index"
```

<!-- Pattern load_vectorstore (RAG-01 + UI-05) -->
```python
@st.cache_resource(show_spinner="Chargement de la base de connaissances...")
def load_vectorstore():
    embeddings = MistralAIEmbeddings(model=EMBEDDING_MODEL)
    return FAISS.load_local(
        FAISS_INDEX_PATH, embeddings, allow_dangerous_deserialization=True
    )
# PITFALL: allow_dangerous_deserialization=True est OBLIGATOIRE — sans ça, ValueError pickle
```

<!-- Pattern session state (UI-02 + UI-03 + UI-04) -->
```python
if "messages" not in st.session_state:
    st.session_state.messages = []           # Liste de dicts {"role": ..., "content": ...}
if "pending_question" not in st.session_state:
    st.session_state.pending_question = None  # Injecté par on_click bouton suggéré
```

<!-- Pattern boutons suggérés (UI-03 + UI-04) -->
```python
def inject_question(q: str):
    st.session_state.pending_question = q
# Affiché UNIQUEMENT si len(st.session_state.messages) == 0
# on_click callback OBLIGATOIRE (pas if st.button() — race condition)
```

<!-- Pattern résolution question active (UI-04) -->
```python
user_input = st.chat_input("Posez votre question sur le profil...")
active_question = st.session_state.pending_question or user_input
if st.session_state.pending_question:
    st.session_state.pending_question = None  # Consommer avant traitement
```

<!-- Pipeline RAG — ordre des messages LangChain (PROMPT-04) -->
```python
# Ordre OBLIGATOIRE : [SystemMessage, HumanMessage, AIMessage, ..., HumanMessage]
# Le HumanMessage de la question courante est ajouté EN DERNIER (après l'historique)
# Ne JAMAIS ajouter la question courante à session_state AVANT l'appel LLM
```

<!-- Gestion erreur 429 (RAG-04) -->
```python
except Exception as e:
    err = str(e)
    if "429" in err or "rate limit" in err.lower() or "too many requests" in err.lower():
        answer = "La limite de requêtes de l'API Mistral a été atteinte. Veuillez réessayer dans quelques instants."
    else:
        answer = f"Une erreur est survenue : {err}"
# L'erreur s'affiche dans la bulle assistant (pas en dehors)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implémenter app.py — pipeline RAG complet et system prompt</name>
  <files>app.py</files>
  <behavior>
    - SYSTEM_PROMPT est une constante string de niveau module exportable
    - SYSTEM_PROMPT contient "français" (PROMPT-01), "professionnel" (PROMPT-02), "ne dispose pas de cette information" ou équivalent (PROMPT-03)
    - load_vectorstore() existe et est décorée avec @st.cache_resource (RAG-01)
    - La liste de messages LangChain commence par SystemMessage, se termine par HumanMessage (PROMPT-04)
    - Toute Exception est attrapée, "429" dans str(e) produit un message français (RAG-04)
  </behavior>
  <action>
Créer `app.py` en suivant exactement les patterns de la section `<interfaces>` ci-dessus. L'app doit tenir sous 120 lignes.

Structure à implémenter dans cet ordre :

**1. Imports** (suivre exactement cette liste — toutes les dépendances sont dans requirements.txt) :
```python
import streamlit as st
from dotenv import load_dotenv
from langchain_community.vectorstores import FAISS
from langchain_core.messages import SystemMessage, HumanMessage, AIMessage
from langchain_mistralai import ChatMistralAI, MistralAIEmbeddings
from config import EMBEDDING_MODEL, LLM_MODEL, K_RETRIEVED, FAISS_INDEX_PATH
```

**2. `load_dotenv()`** — no-op en production Streamlit Cloud, utile en dev local

**3. SYSTEM_PROMPT** — constante de niveau module (testable par test_prompt.py) :
```
Tu es un assistant professionnel qui répond aux questions sur le profil et les compétences d'Etienne Routhier, basé uniquement sur les documents fournis.
Tu réponds toujours en français, avec un ton professionnel et bienveillant.
Si la question dépasse le contenu des documents fournis, réponds explicitement que tu ne disposes pas de cette information — ne génère pas de contenu inventé.
```

**4. `load_vectorstore()`** — décorée `@st.cache_resource(show_spinner="Chargement de la base de connaissances...")` avec `allow_dangerous_deserialization=True` (RAG-01, UI-05)

**5. Initialisation session state** — `messages` et `pending_question` (UI-02, UI-04)

**6. `vectorstore = load_vectorstore()`** — appelé une seule fois

**7. `st.title("Assistant — Dossier de Compétences")`**

**8. Affichage historique** — boucle sur `st.session_state.messages` avec `st.chat_message` (UI-01, UI-02)

**9. Boutons suggérés** — affichés uniquement si `len(st.session_state.messages) == 0`, via `inject_question` callback et `st.columns(2)` (UI-03, UI-04) :
  - Bouton 1 : "Quelles sont vos principales compétences ?"
  - Bouton 2 : "En quoi pouvez-vous m'aider sur mon projet ?"

**10. Résolution question active** — `user_input = st.chat_input(...)` puis `active_question = st.session_state.pending_question or user_input`, puis réinitialisation `pending_question`

**11. Bloc `if active_question:`** :
   a. Afficher message utilisateur avec `st.chat_message("user")`
   b. Append à `st.session_state.messages`
   c. `docs = vectorstore.similarity_search(active_question, k=K_RETRIEVED)` (RAG-02)
   d. `context = "\n\n".join(doc.page_content for doc in docs)`
   e. Construire `lc_messages` = [SystemMessage(content=SYSTEM_PROMPT + f"\n\nContexte:\n{context}"), puis alternance HumanMessage/AIMessage depuis `st.session_state.messages[:-1]`, puis HumanMessage(active_question)] (PROMPT-04)
   f. Dans `st.chat_message("assistant")` avec `st.spinner("...")` : `try/except` autour de `ChatMistralAI(model=LLM_MODEL).invoke(lc_messages).content` (RAG-03, RAG-04)
   g. `st.markdown(answer)` puis append réponse à `st.session_state.messages`

**IMPORTANT :** Ne jamais appeler `st.secrets` explicitement — Streamlit mappe automatiquement `.streamlit/secrets.toml` vers l'environnement. `MistralAIEmbeddings` et `ChatMistralAI` lisent `MISTRAL_API_KEY` depuis l'environnement automatiquement.
  </action>
  <verify>
    <automated>python -m pytest tests/test_prompt.py tests/test_rag.py::test_rag01_load_vectorstore_uses_cache_resource tests/test_rag.py::test_rag02_similarity_search_returns_k_chunks -v 2>&1 | tail -15</automated>
  </verify>
  <done>
- tests/test_prompt.py::test_prompt01_02_03_system_prompt_content PASSED
- tests/test_prompt.py::test_prompt04_history_message_order PASSED
- tests/test_rag.py::test_rag01_load_vectorstore_uses_cache_resource PASSED
- tests/test_rag.py::test_rag02_similarity_search_returns_k_chunks PASSED
- `python -c "import ast; ast.parse(open('app.py').read()); print('syntax OK')"` retourne "syntax OK"
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Vérification manuelle — app.py fonctionnel en local</name>
  <what-built>
app.py complet — pipeline RAG, interface Streamlit, system prompt, gestion d'erreur. L'app est prête à être testée en local.

Prérequis : `MISTRAL_API_KEY` doit être définie dans `.streamlit/secrets.toml` :
```toml
MISTRAL_API_KEY = "sk-..."
```
Ce fichier est dans `.gitignore` — il ne sera pas commité.
  </what-built>
  <how-to-verify>
1. Lancer l'app : `streamlit run app.py`
2. Ouvrir http://localhost:8501 dans un navigateur

**Vérifier les 4 success criteria de la phase :**

a. **UI au démarrage (UI-03, UI-05)** : Deux boutons "Quelles sont vos principales compétences ?" et "En quoi pouvez-vous m'aider sur mon projet ?" sont visibles. Un spinner "Chargement de la base de connaissances..." peut apparaître brièvement au cold start.

b. **Réponse RAG (RAG-01/02/03, PROMPT-01/02/03)** : Cliquer un des boutons OU saisir "Parle-moi de tes compétences en Python". Vérifier :
   - La réponse est en français
   - La réponse mentionne du contenu cohérent avec un dossier de compétences (pas d'hallucination générique)
   - Pour une question hors-scope ("Quel est ton film préféré ?"), le chatbot répond qu'il ne dispose pas de cette information

c. **Multi-tour (PROMPT-04, UI-02)** : Après la première réponse, taper "Dis-m'en plus". Vérifier que la réponse est cohérente avec la question précédente (pas une réponse générique décontextualisée). Vérifier que l'historique des deux échanges est visible.

d. **Gestion erreur (RAG-04)** : Si MISTRAL_API_KEY est invalide ou si le rate limit (2 req/min free tier) est atteint, un message d'erreur en français s'affiche dans la bulle assistant — l'app ne plante pas avec une stack trace.
  </how-to-verify>
  <resume-signal>Taper "approved" si les 4 success criteria sont validés, ou décrire les problèmes observés</resume-signal>
  <action>Lancer `streamlit run app.py` et vérifier les 4 success criteria listés dans how-to-verify.</action>
  <verify>Human verification — voir how-to-verify ci-dessus</verify>
  <done>Les 4 success criteria de la phase sont validés par l'utilisateur</done>
</task>

</tasks>

<verification>
Tests automatisés :
```bash
python -m pytest tests/ -v 2>&1 | tail -20
```

Smoke test RAG-02 (sans Streamlit) :
```bash
python -c "
from langchain_community.vectorstores import FAISS
from langchain_mistralai import MistralAIEmbeddings
from config import EMBEDDING_MODEL, FAISS_INDEX_PATH, K_RETRIEVED
vs = FAISS.load_local(FAISS_INDEX_PATH, MistralAIEmbeddings(model=EMBEDDING_MODEL), allow_dangerous_deserialization=True)
docs = vs.similarity_search('compétences', k=K_RETRIEVED)
print(f'RAG-02 OK: {len(docs)} chunks retournés')
assert len(docs) == K_RETRIEVED
"
```

Validation finale (4 success criteria) : checkpoint:human-verify (Task 2).
</verification>

<success_criteria>
- `app.py` existe et contient moins de 120 lignes
- `python -m pytest tests/ -v` : 0 erreurs, 0 failures (skips acceptés pour RAG-03, RAG-04 manual)
- `streamlit run app.py` démarre sans erreur
- Les 4 success criteria de la phase validés par checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/03-application-streamlit-complete/03-02-SUMMARY.md`
</output>
