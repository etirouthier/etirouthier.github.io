---
phase: 02-build-de-l-index-faiss
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - build_index.py
  - faiss_index/index.faiss
  - faiss_index/index.pkl
autonomous: false
requirements: [INDEX-01, INDEX-02, INDEX-03, INDEX-04]
user_setup:
  - service: mistral
    why: "build_index.py appelle l'API mistral-embed pour générer les embeddings — la clé doit être dans l'environnement avant de lancer le script"
    env_vars:
      - name: MISTRAL_API_KEY
        source: "console.mistral.ai → API Keys — définir avec: export MISTRAL_API_KEY=sk-..."
    dashboard_config: []

must_haves:
  truths:
    - "python build_index.py s'exécute sans erreur et affiche les 4 étapes de progression"
    - "faiss_index/ contient index.faiss et index.pkl après exécution"
    - "Les logs affichent le nombre de chunks générés (attendu ~20) et un aperçu des 3 premiers chunks"
    - "Le texte des chunks est lisible et cohérent (pas de colonnes interleaved, pas de caractères mélangés)"
    - "faiss_index/ est commité dans Git et visible dans git log"
  artifacts:
    - path: "build_index.py"
      provides: "Script offline de construction de l'index FAISS"
      min_lines: 30
    - path: "faiss_index/index.faiss"
      provides: "Index vectoriel binaire FAISS"
    - path: "faiss_index/index.pkl"
      provides: "Métadonnées des chunks (page_content + metadata)"
  key_links:
    - from: "build_index.py"
      to: "config.py"
      via: "from config import EMBEDDING_MODEL, CHUNK_SIZE, CHUNK_OVERLAP, FAISS_INDEX_PATH"
      pattern: "from config import"
    - from: "build_index.py"
      to: "assets/ (PyPDFDirectoryLoader)"
      via: "PyPDFDirectoryLoader('assets/', recursive=True)"
      pattern: "PyPDFDirectoryLoader"
    - from: "build_index.py"
      to: "faiss_index/"
      via: "vectorstore.save_local(FAISS_INDEX_PATH)"
      pattern: "save_local"
---

<objective>
Construire le script offline `build_index.py` qui ingère le PDF source depuis `assets/`, le découpe en chunks, génère les embeddings via l'API Mistral, et sauvegarde l'index FAISS dans `faiss_index/`. Valider qualitativement les chunks via les logs, puis commiter l'index dans Git.

Purpose: L'index FAISS commité est la fondation du pipeline RAG de Phase 3 — sans lui, `app.py` ne peut pas charger le vectorstore au démarrage.
Output: `build_index.py` + `faiss_index/index.faiss` + `faiss_index/index.pkl` dans Git.
</objective>

<execution_context>
@/home/codespace/.claude/get-shit-done/workflows/execute-plan.md
@/home/codespace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/02-build-de-l-index-faiss/02-RESEARCH.md
@.planning/phases/01-setup-configuration/01-01-SUMMARY.md

<interfaces>
<!-- Constantes disponibles depuis config.py (Phase 1) -->
<!-- Importer directement — ne jamais dupliquer ces valeurs dans build_index.py -->

From config.py:
```python
EMBEDDING_MODEL = "mistral-embed"   # Modèle d'embedding Mistral — CRITIQUE: doit être identique entre build et query
LLM_MODEL = "mistral-small-latest"  # Modèle de génération (Phase 3 uniquement)
CHUNK_SIZE = 500                    # Taille des chunks en caractères
CHUNK_OVERLAP = 50                  # Chevauchement en caractères
K_RETRIEVED = 4                     # Nombre de chunks récupérés (Phase 3 uniquement)
FAISS_INDEX_PATH = "faiss_index"    # Chemin relatif du dossier d'index
```

<!-- Fichier source PDF confirmé -->
assets/resume/dossier_competences.pdf  — 4 pages, ~8700 chars, extraction pypdf de bonne qualité (testé)
<!-- Résultat attendu avec CHUNK_SIZE=500, CHUNK_OVERLAP=50 : ~20 chunks -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Créer build_index.py — pipeline complet load→split→embed→save</name>
  <files>build_index.py</files>
  <action>
Créer `build_index.py` à la racine du repo. Ce script est offline (jamais importé par Streamlit) — ne pas importer `streamlit`.

**Structure du script (4 étapes numérotées) :**

```python
# build_index.py
# Script offline — à exécuter une fois pour construire l'index FAISS
# Prérequis : MISTRAL_API_KEY définie dans l'environnement
#   export MISTRAL_API_KEY=sk-...
# Usage : python build_index.py

import os
from dotenv import load_dotenv
from langchain_community.document_loaders import PyPDFDirectoryLoader
from langchain_community.vectorstores import FAISS
from langchain_mistralai import MistralAIEmbeddings
from langchain_text_splitters import RecursiveCharacterTextSplitter
from config import EMBEDDING_MODEL, CHUNK_SIZE, CHUNK_OVERLAP, FAISS_INDEX_PATH

# Charger .env si présent (dev local) — no-op si absent
load_dotenv()

# Étape 1 : Charger tous les PDFs depuis assets/
print("[1/4] Chargement des documents depuis assets/...")
loader = PyPDFDirectoryLoader("assets/", recursive=True)
raw_docs = loader.load()
if not raw_docs:
    raise ValueError("Aucun document trouvé dans assets/ — vérifier que le PDF est présent.")
print(f"[1/4] OK — {len(raw_docs)} page(s) chargée(s)")

# Étape 2 : Découper en chunks
print(f"[2/4] Découpage en chunks (taille={CHUNK_SIZE}, overlap={CHUNK_OVERLAP})...")
splitter = RecursiveCharacterTextSplitter(
    chunk_size=CHUNK_SIZE,
    chunk_overlap=CHUNK_OVERLAP,
)
chunks = splitter.split_documents(raw_docs)
print(f"[2/4] OK — {len(chunks)} chunks générés")

# Étape 3 : Générer les embeddings et construire l'index FAISS
# MistralAIEmbeddings lit MISTRAL_API_KEY depuis l'environnement automatiquement
print(f"[3/4] Génération des embeddings ({EMBEDDING_MODEL}) et construction de l'index FAISS...")
print("      (appel API Mistral — ~20-30 secondes)")
embeddings = MistralAIEmbeddings(model=EMBEDDING_MODEL)
vectorstore = FAISS.from_documents(chunks, embeddings)
print(f"[3/4] OK — index FAISS construit ({len(chunks)} vecteurs)")

# Étape 4 : Sauvegarder l'index
print(f"[4/4] Sauvegarde de l'index dans {FAISS_INDEX_PATH}/...")
vectorstore.save_local(FAISS_INDEX_PATH)
print(f"[4/4] OK — fichiers créés : {FAISS_INDEX_PATH}/index.faiss, {FAISS_INDEX_PATH}/index.pkl")

# Logs de validation (INDEX-04) — lire attentivement avant de commiter l'index
print("\n=== Validation des chunks ===")
print(f"Nombre total de chunks : {len(chunks)}")
n_preview = min(3, len(chunks))
for i in range(n_preview):
    print(f"\n--- Chunk {i+1} ({len(chunks[i].page_content)} chars, source: {chunks[i].metadata.get('source', '?')}) ---")
    print(chunks[i].page_content[:200])

print("\n[IMPORTANT] Vérifier que les chunks ci-dessus sont lisibles et cohérents.")
print("[IMPORTANT] Si le texte semble mélangé ou illisible, NE PAS commiter l'index.")
print("[IMPORTANT] Index prêt à être commité : git add faiss_index/ && git commit -m 'feat(02): add FAISS index'")

# Note pour Phase 3 (app.py) :
# Charger l'index avec allow_dangerous_deserialization=True :
# vectorstore = FAISS.load_local(FAISS_INDEX_PATH, embeddings, allow_dangerous_deserialization=True)
```

**Points critiques :**
- Importer `EMBEDDING_MODEL` depuis `config.py` — jamais dupliquer la valeur `"mistral-embed"` directement
- `load_dotenv()` avant toute instantiation de `MistralAIEmbeddings` — charge `.env` si présent
- Guard `if not raw_docs` avec message d'erreur clair
- Pas de `import streamlit` — ce script est hors contexte Streamlit
  </action>
  <verify>
    <automated>cd /workspaces/etienne.routhier && python -c "
import ast, sys
with open('build_index.py') as f:
    src = f.read()
tree = ast.parse(src)
# Verifier imports critiques
names = [node.names[0].name if hasattr(node, 'names') else node.module for node in ast.walk(tree) if isinstance(node, (ast.Import, ast.ImportFrom))]
assert 'streamlit' not in str(names), 'ERREUR: streamlit importé dans build_index.py'
assert 'config' in str(names), 'ERREUR: config non importé'
print('build_index.py: structure OK (no streamlit, config imported)')
"</automated>
  </verify>
  <done>build_index.py existe à la racine, ne contient pas d'import streamlit, importe depuis config.py, contient les 4 étapes numérotées et les logs de validation.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Valider les chunks et commiter faiss_index/ dans Git</name>
  <what-built>
    build_index.py — script offline complet qui charge assets/resume/dossier_competences.pdf, le découpe en ~20 chunks, génère les embeddings via mistral-embed, et sauvegarde faiss_index/.

    Avant ce checkpoint, vérifier que MISTRAL_API_KEY est définie :
      echo $MISTRAL_API_KEY   (doit afficher sk-... ou similaire)
      export MISTRAL_API_KEY=sk-...   (si non définie)

    Lancer le script :
      python build_index.py

    Durée attendue : ~30 secondes (appel API Mistral)
  </what-built>
  <how-to-verify>
    1. Confirmer que le script s'exécute sans erreur jusqu'à la ligne "[4/4] OK"
    2. Vérifier que les fichiers sont créés :
         ls faiss_index/
         → doit afficher : index.faiss  index.pkl
    3. Lire la section "=== Validation des chunks ===" dans l'output :
         - Le nombre de chunks doit être ~20 (acceptable entre 15 et 30)
         - Les 3 aperçus de chunks doivent contenir du texte français lisible et cohérent
         - Le texte ne doit PAS être un mélange de mots de colonnes différentes
         - Les chunks ne doivent PAS commencer/finir en milieu de phrase de façon aberrante
    4. Vérifier que faiss_index/ n'est PAS ignoré par git :
         git check-ignore faiss_index/
         → doit retourner rien (vide) — confirme que l'index peut être commité
    5. Si tout est bon, commiter l'index :
         git add faiss_index/
         git commit -m "feat(02-01): add FAISS index — 20 chunks, mistral-embed"
    6. Vérifier le commit :
         git log --oneline -3
         → doit montrer le commit "feat(02-01): add FAISS index..."
  </how-to-verify>
  <resume-signal>Taper "approved" si les chunks sont lisibles et l'index est commité. Décrire tout problème observé (texte mélangé, erreur API, nombre de chunks inattendu).</resume-signal>
  <action>Checkpoint humain — voir how-to-verify ci-dessus.</action>
  <verify><automated>git log --oneline -3</automated></verify>
  <done>faiss_index/ commité dans Git, chunks validés comme lisibles et cohérents.</done>
  <files>faiss_index/index.faiss, faiss_index/index.pkl</files>
</task>

</tasks>

<verification>
Phase 2 complète quand :
1. `python -c "import ast; ast.parse(open('build_index.py').read()); print('syntax OK')"` passe sans erreur
2. `python build_index.py` produit `[4/4] OK` et crée `faiss_index/index.faiss` + `faiss_index/index.pkl`
3. Les logs de validation montrent ~20 chunks de texte français lisible
4. `git log --oneline` montre un commit "feat(02-01): add FAISS index..."
5. `git check-ignore faiss_index/` retourne vide (index commitable)
</verification>

<success_criteria>
- `build_index.py` existe, s'exécute sans erreur avec `MISTRAL_API_KEY` définie
- `faiss_index/index.faiss` et `faiss_index/index.pkl` présents et commités dans Git
- Les logs de validation confirment que les chunks sont lisibles (~20 chunks, texte français cohérent)
- Aucun import `streamlit` dans `build_index.py`
- `EMBEDDING_MODEL` importé depuis `config.py` (valeur jamais dupliquée)
</success_criteria>

<output>
Après complétion, créer `.planning/phases/02-build-de-l-index-faiss/02-01-SUMMARY.md`
</output>
