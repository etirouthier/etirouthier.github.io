---
phase: 01-setup-configuration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - config.py
  - requirements.txt
autonomous: true
requirements:
  - SETUP-01
  - SETUP-02
  - SETUP-03

must_haves:
  truths:
    - "git status ne montre pas .streamlit/secrets.toml comme untracked après création du fichier"
    - "from config import EMBEDDING_MODEL, CHUNK_SIZE, CHUNK_OVERLAP, K_RETRIEVED, FAISS_INDEX_PATH réussit sans erreur"
    - "pip install -r requirements.txt s'exécute sans erreur dans un venv propre"
  artifacts:
    - path: ".gitignore"
      provides: "Exclusion de .streamlit/secrets.toml et des artefacts Python locaux"
      contains: ".streamlit/secrets.toml"
    - path: "config.py"
      provides: "Constantes partagées entre build_index.py et app.py"
      exports: ["EMBEDDING_MODEL", "LLM_MODEL", "CHUNK_SIZE", "CHUNK_OVERLAP", "K_RETRIEVED", "FAISS_INDEX_PATH"]
    - path: "requirements.txt"
      provides: "Dépendances avec versions fixées compatibles Streamlit Community Cloud"
      contains: "streamlit=="
  key_links:
    - from: "build_index.py (Phase 2)"
      to: "config.py"
      via: "from config import EMBEDDING_MODEL, ..."
      pattern: "from config import"
    - from: "app.py (Phase 3)"
      to: "config.py"
      via: "from config import EMBEDDING_MODEL, ..."
      pattern: "from config import"
---

<objective>
Mettre en place les trois fichiers de configuration fondamentaux du projet : `.gitignore` (sécurité des secrets), `config.py` (constantes partagées), et `requirements.txt` (dépendances fixées). Ces fichiers constituent le socle que toutes les phases suivantes utilisent sans les recréer.

Purpose: Aucun secret ne peut être accidentellement commité ; toutes les constantes critiques sont définies une seule fois ; les dépendances sont reproductibles sur Streamlit Community Cloud.
Output: Trois fichiers de configuration prêts à l'emploi, commitables dans Git.
</objective>

<execution_context>
@/home/codespace/.claude/get-shit-done/workflows/execute-plan.md
@/home/codespace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-setup-configuration/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Créer .gitignore — sécurité des secrets avant tout</name>
  <files>.gitignore</files>
  <action>
Créer le fichier `.gitignore` à la racine du repo avec le contenu exact suivant (ordre important : secrets en premier pour la lisibilité) :

```
# Secrets Streamlit — jamais commités
# Source: https://docs.streamlit.io/deploy/streamlit-community-cloud/deploy-your-app/secrets-management
.streamlit/secrets.toml

# Variables d'environnement locales
.env
*.env

# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
dist/
build/
*.egg

# Environnements virtuels
venv/
.venv/
env/
ENV/

# IDE
.idea/
.vscode/
*.swp
*.swo

# macOS
.DS_Store

# NOTE: faiss_index/ est INTENTIONNELLEMENT absent de ce .gitignore
# Il doit être commité pour être disponible sur Streamlit Community Cloud
```

CRITIQUE : ne pas ajouter `faiss_index/` dans ce fichier — l'index FAISS doit être commitable (filesystem Streamlit Community Cloud est éphémère). Toute entrée excluant `*.pkl`, `*.faiss`, ou `faiss_index/` doit être absente.

Après création, valider :
1. Créer `.streamlit/secrets.toml` avec contenu `MISTRAL_API_KEY = "test"` si le dossier n'existe pas
2. Lancer `git check-ignore -v .streamlit/secrets.toml` — doit retourner `.gitignore:2:.streamlit/secrets.toml`
3. `git status` ne doit PAS montrer `.streamlit/secrets.toml` dans les fichiers untracked
  </action>
  <verify>
    <automated>mkdir -p .streamlit && echo 'MISTRAL_API_KEY = "test"' > .streamlit/secrets.toml && git check-ignore -v .streamlit/secrets.toml</automated>
  </verify>
  <done>.streamlit/secrets.toml exclu par .gitignore — git check-ignore retourne la ligne correspondante. faiss_index/ n'est pas mentionné dans .gitignore.</done>
</task>

<task type="auto">
  <name>Task 2: Créer config.py — constantes partagées entre scripts</name>
  <files>config.py</files>
  <action>
Créer le fichier `config.py` à la racine du repo avec exactement ces constantes :

```python
# config.py
# Constantes partagées entre build_index.py et app.py
# IMPORTANT: Importer depuis ce module — ne jamais dupliquer ces valeurs dans d'autres scripts
# Toute divergence du modèle d'embedding entre build et query rend l'index inutilisable

# Modèle d'embedding — DOIT être identique entre build_index.py et app.py
# Si ces valeurs diffèrent, FAISS retourne des résultats silencieusement invalides
EMBEDDING_MODEL = "mistral-embed"

# Modèle de génération
LLM_MODEL = "mistral-small-latest"

# Paramètres de chunking (calibrés pour un document court et dense)
# Ajustable en Phase 2 selon qualité des chunks observée dans les logs
CHUNK_SIZE = 500       # caractères
CHUNK_OVERLAP = 50     # caractères — chevauchement pour continuité du contexte

# Nombre de chunks récupérés par requête
# Compromis précision/contexte pour Mistral free tier
K_RETRIEVED = 4

# Chemin de l'index FAISS (relatif à la racine du repo)
FAISS_INDEX_PATH = "faiss_index"
```

Ce fichier ne contient que des constantes — pas d'imports, pas de logique, pas d'appels réseau. Il doit être importable depuis n'importe quel script sans modifier le PYTHONPATH (les deux scripts `build_index.py` et `app.py` seront à la racine du repo, au même niveau que `config.py`).
  </action>
  <verify>
    <automated>python -c "from config import EMBEDDING_MODEL, LLM_MODEL, CHUNK_SIZE, CHUNK_OVERLAP, K_RETRIEVED, FAISS_INDEX_PATH; print('OK:', EMBEDDING_MODEL, LLM_MODEL, CHUNK_SIZE, CHUNK_OVERLAP, K_RETRIEVED, FAISS_INDEX_PATH)"</automated>
  </verify>
  <done>Import réussit et affiche : OK: mistral-embed mistral-small-latest 500 50 4 faiss_index</done>
</task>

<task type="auto">
  <name>Task 3: Créer requirements.txt — dépendances avec versions fixées</name>
  <files>requirements.txt</files>
  <action>
Générer `requirements.txt` en installant les dépendances dans l'environnement courant puis en capturant les versions exactes via `pip freeze`.

Séquence exacte à exécuter :

1. Installer toutes les dépendances nécessaires pour les 4 phases du projet :
```bash
pip install streamlit mistralai langchain langchain-core langchain-community langchain-mistralai langchain-text-splitters faiss-cpu pypdf numpy
```

2. Capturer les versions exactes installées (opérateur `==`, pas `>=`) :
```bash
pip freeze > requirements.txt
```

3. Vérifier que `requirements.txt` contient au moins ces packages (les noms exacts peuvent varier selon la résolution pip) :
   - `streamlit`
   - `mistralai`
   - `langchain`
   - `langchain-core`
   - `langchain-community`
   - `langchain-mistralai`
   - `langchain-text-splitters`
   - `faiss-cpu`
   - `pypdf`
   - `numpy`

NE PAS écrire les versions manuellement — utiliser uniquement `pip freeze`. Ne pas filtrer les dépendances transitives — le fichier complet garantit la reproductibilité sur Streamlit Community Cloud.

NE PAS utiliser `python-dotenv` — `st.secrets` couvre ce besoin sans package supplémentaire.
  </action>
  <verify>
    <automated>python -c "import streamlit, mistralai, langchain, langchain_core, langchain_community, langchain_mistralai, langchain_text_splitters, faiss, pypdf, numpy; print('All imports OK')" && grep -c "==" requirements.txt</automated>
  </verify>
  <done>Tous les imports réussissent et requirements.txt contient des entrées avec versions fixées (opérateur ==). La commande grep retourne un nombre > 0.</done>
</task>

</tasks>

<verification>
Vérification globale de la Phase 1 :

1. Sécurité secrets : `git check-ignore -v .streamlit/secrets.toml` retourne une correspondance dans `.gitignore`
2. Importabilité config : `python -c "from config import EMBEDDING_MODEL, CHUNK_SIZE, CHUNK_OVERLAP, K_RETRIEVED, FAISS_INDEX_PATH; print('OK')"` réussit
3. Intégrité requirements : `pip install -r requirements.txt` s'exécute sans erreur (validation dans l'environnement existant — un venv propre est la validation définitive)
4. Contre-vérification gitignore : créer temporairement `faiss_index/.gitkeep` et confirmer que `git status` le liste comme untracked (pas exclu)

```bash
git check-ignore -v .streamlit/secrets.toml && \
python -c "from config import EMBEDDING_MODEL, CHUNK_SIZE, CHUNK_OVERLAP, K_RETRIEVED, FAISS_INDEX_PATH; print('config OK')" && \
pip install -r requirements.txt --quiet && echo "requirements OK"
```
</verification>

<success_criteria>
- `.gitignore` exclut `.streamlit/secrets.toml` — `git status` après création du fichier ne le montre pas comme untracked
- `config.py` est importable depuis la racine du repo avec toutes les constantes (EMBEDDING_MODEL, LLM_MODEL, CHUNK_SIZE, CHUNK_OVERLAP, K_RETRIEVED, FAISS_INDEX_PATH)
- `requirements.txt` liste toutes les dépendances avec versions fixées (opérateur ==) et est installable via `pip install -r requirements.txt` sans erreur
- `faiss_index/` n'est PAS dans `.gitignore` (contre-vérification critique pour Phase 2)
</success_criteria>

<output>
After completion, create `.planning/phases/01-setup-configuration/01-01-SUMMARY.md` with:
- Files created and their content summary
- Actual versions captured in requirements.txt (top 10 packages)
- Verification results (all 4 checks)
- Any deviations from plan (e.g., package names that differed)
- Decisions carried forward to Phase 2 (chunk size adjustable, faiss_index/ committed)
</output>
