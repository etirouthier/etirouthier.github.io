---
phase: 06-identite-visuelle
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app.py
autonomous: false
requirements:
  - BRAND-01
  - ACCU-01

must_haves:
  truths:
    - "Le header affiche 'Etienne Routhier' et 'Consultant Freelance — Data & IA' en haut de page"
    - "Le header est lisible en thème clair et en thème sombre (couleur via CSS variable, pas de hex hardcodé)"
    - "Un message d'accueil de l'assistant est visible au premier chargement avant tout chip de suggestion"
    - "Le message d'accueil disparaît après l'envoi du premier message et ne réapparaît pas"
  artifacts:
    - path: "app.py"
      provides: "HTML header + welcome message dans le guard messages == 0"
      contains: "HEADER_HTML"
  key_links:
    - from: "HEADER_HTML constant"
      to: "st.markdown(HEADER_HTML, unsafe_allow_html=True)"
      via: "appel isolé — jamais combiné avec d'autre markdown"
      pattern: "st\\.markdown\\(HEADER_HTML"
    - from: "if len(st.session_state.messages) == 0"
      to: "st.chat_message(\"assistant\") welcome bubble"
      via: "premier statement dans le guard, avant cols = st.columns(4)"
      pattern: "with st\\.chat_message\\(\"assistant\"\\)"
---

<objective>
Ajouter l'identité visuelle d'Étienne Routhier à l'app Streamlit déployée : un header HTML centré avec son nom et son titre professionnel, et un message d'accueil de l'assistant qui s'affiche uniquement au premier chargement.

Purpose: Un visiteur comprend immédiatement qui est Étienne et ce que le chatbot peut faire pour lui, sans avoir à poser une première question pour comprendre le contexte.
Output: app.py mis à jour (114 lignes ~) avec HEADER_HTML constant, st.markdown isolé remplaçant st.title, et st.chat_message("assistant") welcome bubble dans le guard messages == 0.
</objective>

<execution_context>
@/home/codespace/.claude/get-shit-done/workflows/execute-plan.md
@/home/codespace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Header HTML — remplacer st.title par HEADER_HTML constant + st.markdown isolé (BRAND-01)</name>
  <files>app.py</files>
  <action>
Dans app.py :

1. Ajouter la constante HEADER_HTML après SYSTEM_PROMPT (ligne 23), avant le décorateur @st.cache_resource :

```python
HEADER_HTML = """
<div style="text-align: center; padding: 1rem 0 0.5rem 0;">
    <h1 style="margin: 0; font-size: 1.8rem; font-weight: 700;">Etienne Routhier</h1>
    <p style="margin: 0.25rem 0 0 0; font-size: 1rem;
              color: var(--text-color); opacity: 0.65;">
        Consultant Freelance — Data &amp; IA
    </p>
</div>
"""
```

Règles critiques :
- Utiliser `var(--text-color)` avec `opacity: 0.65` — jamais de hex hardcodé (#666 etc.) qui serait illisible en dark mode
- Écrire `&amp;` dans le HTML (pas `&`) pour éviter l'entité HTML non échappée
- HEADER_HTML doit être défini comme constante module-level, au même niveau que SYSTEM_PROMPT et SUGGESTIONS

2. Remplacer la ligne 47 :
```python
# SUPPRIMER :
st.title("Assistant — Dossier de Compétences")

# REMPLACER PAR (appel isolé — ne jamais combiner avec d'autre markdown dans le même appel) :
st.markdown(HEADER_HTML, unsafe_allow_html=True)
```

Anti-patterns à éviter :
- `st.html(HEADER_HTML)` : strip les `<style>` tags via DOMPurify depuis Streamlit 1.38+ — ne pas utiliser
- Concatener HEADER_HTML avec d'autres strings markdown dans le même `st.markdown` : bug de rendu Streamlit 1.46+ (GitHub #11888)
  </action>
  <verify>
    <automated>cd /workspaces/etienne.routhier && python -c "import ast; ast.parse(open('app.py').read()); print('syntax OK')"</automated>
  </verify>
  <done>app.py ne contient plus st.title(...), HEADER_HTML est défini comme constante module-level, st.markdown(HEADER_HTML, unsafe_allow_html=True) est présent en appel isolé, la couleur du subtitle utilise var(--text-color), le caractère & est encodé &amp;</done>
</task>

<task type="auto">
  <name>Task 2: Welcome message — insérer st.chat_message("assistant") en premier dans le guard messages == 0 (ACCU-01)</name>
  <files>app.py</files>
  <action>
Dans app.py, modifier le block `if len(st.session_state.messages) == 0:` pour insérer le welcome bubble comme premier statement, avant `cols = st.columns(4)`.

Structure cible :

```python
# Boutons suggérés — affichés uniquement avant le premier message
if len(st.session_state.messages) == 0:
    with st.chat_message("assistant"):
        st.markdown(
            "Bonjour ! Je suis l'assistant d'Etienne Routhier, "
            "consultant freelance Data & IA. "
            "Je peux répondre à vos questions sur ses compétences techniques, "
            "ses missions passées et ses disponibilités, "
            "à partir de son dossier de compétences. "
            "Posez votre question ou choisissez une suggestion ci-dessous."
        )
    cols = st.columns(4)
    for i, (col, question) in enumerate(zip(cols, SUGGESTIONS)):
        with col:
            st.button(
                question,
                key=f"suggestion_{i}",
                on_click=inject_question,
                args=(question,),
            )
```

Règles critiques :
- Le `with st.chat_message("assistant"):` doit être le PREMIER statement dans le guard — avant `cols = st.columns(4)` — pour que le bubble apparaisse au-dessus des chips
- Ne PAS ajouter de variable session state supplémentaire pour contrôler la visibilité — le guard `messages == 0` suffit (ajout d'état redondant qui peut diverger)
- Ne PAS utiliser `st.info(...)` ou `st.markdown(...)` pour le welcome — `st.chat_message("assistant")` correspond exactement au visual pattern de la conversation
- Le message ne doit pas répéter "Je suis Etienne Routhier" (le header s'en charge) mais peut référencer son nom en contexte
  </action>
  <verify>
    <automated>cd /workspaces/etienne.routhier && python -c "import ast; ast.parse(open('app.py').read()); print('syntax OK')"</automated>
  </verify>
  <done>Le guard `messages == 0` contient un `with st.chat_message("assistant"):` comme premier statement, suivi du bloc cols/chips existant. La syntaxe Python est valide. Le message explique le périmètre du chatbot sans répéter le titre du header.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Header HTML centré avec "Etienne Routhier" + "Consultant Freelance — Data & IA" (via HEADER_HTML constant), et message d'accueil de l'assistant visible au premier chargement dans le guard messages == 0.</what-built>
  <how-to-verify>
1. Lancer l'app localement : `streamlit run app.py`
2. Vérifier le header : "Etienne Routhier" en H1 centré, "Consultant Freelance — Data & IA" en sous-titre muted visible
3. Vérifier le welcome message : bulle assistant visible au-dessus des 4 chips de suggestion
4. Envoyer un message : confirmer que la bulle d'accueil et les chips disparaissent, l'historique prend leur place
5. Basculer en dark mode (engrenage Streamlit > Settings > Theme > Dark) : confirmer que le sous-titre est lisible
6. Si tout est correct : commiter et pousser sur main pour déploiement automatique Streamlit Cloud (https://etirouthierappio.streamlit.app/)
  </how-to-verify>
  <resume-signal>Tapez "approved" si header + welcome message sont corrects en light et dark mode, ou décrivez les ajustements visuels souhaités (taille police, texte du message, etc.)</resume-signal>
</task>

</tasks>

<verification>
Verification complète de la phase 6 :
- `python -c "import ast; ast.parse(open('app.py').read()); print('syntax OK')"` passe sans erreur
- `st.title(...)` n'apparaît plus dans app.py
- `HEADER_HTML` est défini comme constante et contient `var(--text-color)` et `&amp;`
- `st.markdown(HEADER_HTML, unsafe_allow_html=True)` est présent en appel isolé
- `with st.chat_message("assistant"):` est le premier statement dans le guard `messages == 0`
- Validation visuelle : header visible, welcome message visible avant premier message, absent après
</verification>

<success_criteria>
1. Le header affiche "Etienne Routhier" et "Consultant Freelance — Data & IA" en haut de page
2. Un message d'accueil de l'assistant est visible au premier chargement, avant les chips, expliquant le périmètre du chatbot
3. Le message d'accueil disparaît après l'envoi du premier message et ne réapparaît pas
4. Le header est lisible en thème clair et en thème sombre
</success_criteria>

<output>
After completion, create `.planning/phases/06-identite-visuelle/06-01-SUMMARY.md`
</output>
